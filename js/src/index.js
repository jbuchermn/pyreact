import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import PyreactRoot from './PyreactRoot';

ReactDOM.render(
  <React.StrictMode>
    <PyreactRoot />
  </React.StrictMode>,
  document.getElementById('root')
);
